name = "cloudmail-gateway"
main = "src/index.ts"
compatibility_date = "2026-09-01"

[vars]
# 不要把真实邮箱凭据写入这里。
UPSTREAM_API = "https://email.kodao.site"
# ALLOWED_ORIGIN = "https://your-desktop-web-shell.example.com"

# 可选：通过 wrangler secret put GATEWAY_TOKEN 设置，不要提交真实值。
